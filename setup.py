from setuptools import setup, find_packages

setup(
    name="EggTools",
    version="1.0.0",
    packages=find_packages(),
    description="A simple Python library that lets you update and install my modules, scan python code for potential malicious code and crash python.",
    author="",
    python_requires=">=3.7",
)
